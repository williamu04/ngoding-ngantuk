#include <iostream>
using namespace std;

// Menjalankan program hello world
int main(){
    cout << "Hello World";
}

// Deklarasi & Definisi dalam c++
char ch;
int count = 1;
char* name = "Yes";
struct complex {float re, im;};
complex cvar;
extern complex sqrt(complex);
extern int error_number;
typedef complex point;
float real(complex* p) {return p->re;};
const double pi = 3.14;
struct user;
template <class T> abs (T a) {return a<0 ? -a : a;}
enum beer {Dunhil, William, Putra};



